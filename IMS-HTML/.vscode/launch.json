{
    "version": "0.2.0",
    "configurations": [
        {
        "type": "chrome",
        "request": "launch",
        "name": "Launch web page",
        "url": "http://localhost/${relativeFile}",
        "webRoot": "${workspaceFolder}"
        }
    ]
}